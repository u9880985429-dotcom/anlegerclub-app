{
  "name": "@traderiq/db",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'db package: schema-only in Phase 1'",
    "lint": "echo 'no lint'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist node_modules/.cache"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
